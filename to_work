D: start
заливай
увидел?
я добавил стандартные библиотеки потому что ниже написал доп код для проверки
код для примеров -- это все ниже int main() и он сам, наерное это не надо для самого решения, а больше для самопроверки. Думаю надо удалить
сейчас решения без примеров, только код решения
если нужны примеры то можно взять int main() и ниже из depse1.cpp, подойдет в любой код, только надо еще библиотеки добавить 
#include <iostream>
#include <vector>
#include <string>
